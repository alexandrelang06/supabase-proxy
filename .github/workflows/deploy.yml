name: Déploiement Azure Functions

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v3

      - name: 📦 Générer le ZIP de déploiement
        run: |
          zip -r functionapp-deploy.zip auth rest host.json package.json package-lock.json \
            -x "node_modules/*" ".github/*" "local.settings.json" "*.zip" "zip-inspect/*"

      - name: 🚀 Déployer sur Azure sans build
        uses: azure/functions-action@v1
        with:
          app-name: itfw-proxy-func
          publish-profile: ${{ secrets.AZURE_FUNCTION_PUBLISH_PROFILE }}
          package: functionapp-deploy.zip
          scm-do-build-during-deployment: false
          enable-oryx-build: false
#