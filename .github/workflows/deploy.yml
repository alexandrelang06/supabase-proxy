name: Déploiement Azure Functions

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # 1) Récupère ton code
      - name: 📥 Checkout
        uses: actions/checkout@v3

      # 2) Installe tes dépendances (pas de build)
      - name: 🧩 npm install
        run: npm install

      # 3) Prépare l’archive ZIP sans node_modules ni secrets
      - name: 📦 Créer functionapp.zip
        run: |
          zip -r functionapp.zip . \
            -x ".github/*" "proxy.zip" "local.settings.json"

      # 4) Déploie via le publish-profile stocké dans tes secrets
      - name: 🚀 Déploiement Azure Functions
        uses: azure/functions-action@v1
        with:
          app-name: itfw-proxy-func
          publish-profile: ${{ secrets.AZURE_FUNCTION_PUBLISH_PROFILE }}
          package: functionapp.zip
          scm-do-build-during-deployment: false
          enable-oryx-build: false
